{
  "name": "android-sim-mcp",
  "version": "0.1.0",
  "description": "MCP server for driving the Android emulator in agentic feedback loops",
  "type": "module",
  "bin": {
    "android-sim-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}